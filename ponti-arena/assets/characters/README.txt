Drop optional character photos here using the exact filenames listed in
the main README.md ("Adding Character Images" section), e.g. knight.png,
archer.png, wizard.png, etc.

This folder can stay empty - the game generates a themed icon for every
character automatically when no photo is found.
