Reserved for future real audio files. By default Ponti Arena synthesizes
all music and sound effects at runtime using the Web Audio API (see
js/audio.js), so no files are required here for the game to have full audio.
